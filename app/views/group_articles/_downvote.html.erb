<%= form_for(current_user.badgrouparticlerelations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :article_id, group_article.id %></div>
  <%= f.submit "Downvote", class: "btn btn-primary" %>
<% end %>
