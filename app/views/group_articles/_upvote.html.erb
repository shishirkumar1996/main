<%= form_for(current_user.grouparticlerelations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :article_id, group_article.id %></div>
  <%= f.submit "Upvote", class: "btn btn-primary" %>
<% end %>
