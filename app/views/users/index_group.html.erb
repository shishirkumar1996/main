<%= render :partial=>'layouts/header' %>
<div row='col-md-5 col-md-offset-2'>
	<% @groups.each do |group| %>
		<h1><%= link_to group.name ,group_path(group.id) %></h1>
		</br>
		<h6><%= group.users.count %> Members</h6>
		<br>
	<% end %>

</div>
