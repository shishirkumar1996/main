<%= form_for(current_user.badanswerrelations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :answer_id, answer.id %></div>
  <%= f.submit "Downvote", class: "btn btn-primary" %>
<% end %>
