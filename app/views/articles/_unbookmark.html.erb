<%= form_for(current_user.article_bookmark_relations.find_by(article_id: article.id),html: {method: :delete},remote: true) do |f| %>
	<%= f.submit "Unbookmark",class: 'btn common btn-action' %>
<% end %>
