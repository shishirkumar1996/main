<%= form_for(current_user.badarticlerelations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :article_id, article.id %></div>
  <%= f.submit "Downvote", class: "btn btn-primary" %>
<% end %>
