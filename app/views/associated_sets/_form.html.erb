<%= form_for(associated_set) do |f| %>
  <% if associated_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(associated_set.errors.count, "error") %> prohibited this associated_set from being saved:</h2>

      <ul>
      <% associated_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
