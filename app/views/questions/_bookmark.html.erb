<%= form_for(current_user.question_bookmark_relations.build,remote: true) do |f| %>
	<div><%= hidden_field_tag :question_id,question.id %></div>
	<%= f.submit "Bookmark",class: 'btn-common btn-action' %>
<% end %>
