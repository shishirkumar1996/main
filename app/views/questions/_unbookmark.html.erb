<%= form_for(current_user.question_bookmark_relations.find_by(question_id: question.id),html: {method: :delete},remote: true) do |f| %>
	<%= f.submit "Unbookmark",class: 'btn-common btn-action' %>
<% end %>
