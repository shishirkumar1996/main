<%= form_for(current_user.groupanswerrelations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :answer_id, groupanswer.id %></div>
  <%= f.submit "Upvote", class: "btn btn-primary" %>
<% end %>
