var replyContainer = $('#new-reply-container');
replyContainer.html("<%=j render(@article_reply) %>").promise().done(function(){
  $('.submit-article-reply').slideUp(400, function () {
    replyContainer.slideDown(400);
  });
});
